<section data-component="processing" class="shopping">
    <div class="processing__title">
        <div class="container">
            <h3>
                <a href="{{ fn('get_permalink', wc_shop) }}">
                    <i class="icon-angle-right"></i>{{ post.title }}

                    {% if fn('is_checkout') %}
                        <span>{{ __('Rond eenvoudig af binnen 3 stappen') }}</span>
                    {% endif %}
                </a>
            </h3>

            {% if fn('is_cart') %}
                <p><i class="icon-clock"></i> {{ __('1 minuut') }}</p>
            {% endif %}
        </div>
    </div>

    <div class="processing__holder">
        <div class="container">
            {{ post.content }}
        </div>

        {% if fn('is_cart') %}
            {% include 'components/customers' %}
        {% endif %}
    </div>
</section>

